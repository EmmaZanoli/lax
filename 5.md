Implementa la schermata /magazzino, dedicata alla visibilità sulle giacenze.

- In cima tre card di sintesi: "Pezzi in magazzino ora" (somma dei residui), "Ancora da consegnare" (somma di ordinato − ritirato), "Prodotti scoperti" (quanti hanno delta < 0, in terracotta se > 0).
- Se ci sono ammanchi, un banner in evidenza che li elenca.
- Una legenda: Ritirati / Da consegnare / Cuscinetto libero / Scoperto.
- Una riga per prodotto con una BARRA DI CAPACITÀ che scompone la giacenza. La lunghezza di riferimento è max(giacenza iniziale, ordinato). Segmenti da sinistra: pezzi già ritirati (grigio spento), pezzi ancora da consegnare coperti dalla merce fisica (ottone), la parte scoperta se gli ordini superano la merce (terracotta a righe), e l'eventuale cuscinetto libero oltre gli ordini (verde salvia). A destra il numero grande è il residuo in magazzino ora, con una spia (chip) che dice "coperto (+margine)" oppure "mancano N".
- Aggiungi alla voce Magazzino della colonna laterale un badge in terracotta col numero di prodotti scoperti (quando > 0).

Calcoli, per prodotto: daConsegnare = ordered − pickedUp; coperto = min(daConsegnare, max(0, initialStock − pickedUp)); scoperto = max(0, daConsegnare − coperto); cuscinetto = max(0, initialStock − ordered). Usa i selettori esistenti.